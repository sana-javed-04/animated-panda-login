# 🐼 Animated Interactive Login Portal

A highly creative, single-page interactive authentication form utilizing native HTML5 video API state triggers. The application features an animated character workspace that dynamically changes visual framing sequences based on active user input parameters.

---

## 🛠️ Tech Stack Used

* **HTML5:** Modular interface structuring (`.cont`, `.vido-cont`, native `.mp4` video wrappers).
* **CSS3 Engine:** External layout architecture tokens (`Poppins`), modern glassmorphic layouts, embedded Google Material design vectors (`Material Symbols`), and responsive fluid container scaling maps.
* **Vanilla JavaScript (ES6+):** Targeted asynchronous timer components (`setTimeout`), custom programmatic video control systems (`currentTime`/`play`/`pause`), state-driven toggle loops, and input interaction trackers.

---

## ✨ Key Features Included

* **Contextual Focus Tracking:** Custom input event handlers toggle sequential framing timelines programmatically based on active input targets (`focus`/`blur`).
* **Dynamic Password Guard:** Toggling security states (`text` vs `password`) alters program flow to swap interactive framing loops dynamically.
* **Asynchronous Animation Blocks:** Embedded calculation engines convert real-time timestamp variables into execution streams, freezing tracking elements precisely.
* **Native Media Controls:** 0% external animation libraries needed; standard native HTML5 media features power all transitions.

---

## 📁 Repository Directory Structure

```text
├── form.html      # Main interface architecture workspace
├── form.css       # Design tokens, component grids, and responsive parameters
├── form.js        # Logical runtime scheduler and interactive tracking engine
└── panda.mp4      # Core functional system animation element
```

> **Note on Asset Integration:** All resources and media objects are maintained directly at the root pipeline architecture layer to guarantee lightning-fast execution and 100% asset uptime on live remote servers.

---

## 💻 Local Workspace Startup

1. **Clone** this specific repository path to your local environment directory.
2. Launch the `form.html` dashboard portal inside any standard web browser engine.


---

## 👩‍💻 About the Developer
- **Name:** Sana Javed
- **Role:** Frontend Developer & WordPress Expert
- **Education:** BS Computer Science
- **Profiles:** [GitHub](https://github.com/sana-javed-04/) | [LinkedIn](https://www.linkedin.com/in/sana-javed-dev/) | [Fiverr](https://www.fiverr.com/users/sanajaved_dev/)

---
